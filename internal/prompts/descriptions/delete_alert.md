Permanently delete a Last9 alert rule from its owning alert group. Use get_alert_config first to verify entity_id, the exact rule ID, notification coverage, runbook/links, and whether another rule depends on its KPI. Summarize the operational impact and only call this tool after the user explicitly confirms deletion. Deletion may require delete-scoped credentials and does not promise cleanup of a separately managed KPI. Returns deletion status and a link to Alerting.
