List Alert Hygiene Pulse runs for the configured organization.

Use `limit` from 1 to 100 and pass each opaque `next_cursor` back as `cursor` until empty when complete coverage is needed. Preserve analysis and delivery status exactly; delivery failure does not change analysis truth.
