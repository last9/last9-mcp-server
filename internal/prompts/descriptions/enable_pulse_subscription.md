Enable one Alert Hygiene Pulse subscription by `subscription_id`.

Read the subscription first and confirm its configuration is the one the user intends to activate. This write changes only the enabled state and requires the server's explicit `pulse_manage` grant, user approval, and `confirmed: true`.
