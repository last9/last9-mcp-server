Disable one Alert Hygiene Pulse subscription by `subscription_id`.

Read the subscription first and ask the user before disabling its scheduled delivery. This write changes only the enabled state and requires the server's explicit `pulse_manage` grant, user approval, and `confirmed: true`.
