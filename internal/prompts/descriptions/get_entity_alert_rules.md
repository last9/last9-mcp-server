Fetch expression logic and resolved PromQL for an entity's alert rules. Use get_alert_config first to discover the entity_id. An optional severity filters the rules. Returns rule identity, expression and evaluation conditions, window, and indicators with resolved PromQL and units.