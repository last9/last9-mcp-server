Get one Alert Hygiene Pulse finding occurrence by `run_id` and `occurrence_id`.

Pass the `id` field returned by `list_pulse_findings` as `occurrence_id`; do not substitute `finding_id`. Preserve the finding's history, source coverage, and status exactly.
