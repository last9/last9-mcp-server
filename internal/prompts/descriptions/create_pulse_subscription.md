Create a disabled Alert Hygiene Pulse subscription for the configured organization.

Provide the complete name, cron schedule, IANA timezone, recipient list, alert scope, measured analysis configuration, and configuration version. Never infer these values. Creation is always disabled; enablement is a separate action. This write requires the server's explicit `pulse_manage` grant, user approval, and `confirmed: true`.
